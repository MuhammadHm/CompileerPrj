function f(var x=4 , var y=5 ,var z){
    if (x) {  return 0; }
    else   {  return y; }

    var x[]={1,5,6}, y=x+1, q , z[5];

    while ( x!=0 ) {
       x=x+1;
    }

    for( var j=1; j<5; j=j+1 ) {
       for( var i : array ){
            array=i;
       }
    }
    do{
         sum(x,y);
     }
     while( sum < 10 );
    switch(z){
        case "1" : { return 0; }
        default : {      }
    }
    var myObj = {
          "name": "John",
          "age":30,
          "cars": [
            { "name": "Ford", "models":[ "Fiesta", "Focus" , "Mustang" ] },
            { "name": "BMW", "models":[ "320", "X3", "X5" ] },
            { "name": "Fiat", "models":[ "500", "Panda" ] }
          ]
        };
}
function f(var x,var y){

    sort(arr,function(x,y){
        return a>b;
    });

    var x=sum(x,y);
}


Select * from x where x=3;

select f(x,y) from t;

Select * from selec;

insert into table1 values(x,y);
